# CRYCHIC-MCP runtime dependencies.
# Tier-1 (Xue 2024) extras live in adrd_nmed2024/requirements.txt; the imaging
# stack (torch/monai/nibabel) is shared with it.

# --- MCP server + data contracts ---
mcp>=1.2.0
pydantic>=2.6

# --- web UI (FastAPI front end over the live pipeline) ---
fastapi>=0.110
uvicorn[standard]>=0.27

# --- LLM transport (Nemotron, OpenAI-compatible) ---
httpx>=0.27

# --- imaging + numerics ---
torch>=2.1
torchvision>=0.16
monai>=1.3
nibabel>=5.0
numpy>=1.24
pandas>=2.0

# --- optional: attribution heatmap PNG (best-effort in adrd_tool) ---
matplotlib>=3.7
seaborn>=0.12
